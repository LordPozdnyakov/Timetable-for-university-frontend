const GET_USER = "GET_USER";



let initialState = {

}


// @ts-ignore
export const ProfileReducer = (state = initialState, action) => {

    switch (action.type) {
        // @ts-ignore
        case GET_USER: {

        }
        default:
            return state;
    }

}

// @ts-ignore
export default getUserProfile = () => (dispatch) => {

}
