export default interface TeacherFormInfo {
  id: number | null;
  firstName: string;
  lastName: string;
  patronymic: string;
  birthDay: string;
  phoneNumber: string;
  email: string;
  address: string;
}
